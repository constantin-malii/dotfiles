{
  "alias": "Ceiling: Set Volume",
  "description": "Set the ceiling speakers volume to an EXACT ABSOLUTE level (0-100). Use ONLY when the user states an explicit target number, e.g. 'set volume to 40' or 'volume to 70 percent'. NEVER use this for relative changes ('up/down/louder/quieter/by N percent') — use Volume Up / Volume Down for those.",
  "fields": {
    "volume": {
      "description": "Absolute volume percent 0-100",
      "name": "Volume",
      "required": true,
      "selector": {
        "number": {
          "max": 100,
          "min": 0,
          "mode": "box"
        }
      }
    }
  },
  "icon": "mdi:volume-high",
  "object_id": "ceiling_set_volume",
  "sequence": [
    {
      "action": "rest_command.resolver_command",
      "continue_on_error": true,
      "data": {
        "intent": "interaction",
        "params": {
          "mode": "set_volume",
          "volume": "{{ volume }}"
        }
      },
      "response_variable": "r"
    }
  ]
}
