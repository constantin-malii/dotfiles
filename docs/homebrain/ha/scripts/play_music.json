{
  "alias": "Play Music",
  "description": "Play music on the ceiling speakers from the LOCAL music library. 'query' is what to play: a song title, artist, album, or playlist name.",
  "fields": {
    "media_type": {
      "description": "Optional. LEAVE EMPTY for a song title or when unsure (the system figures it out). Only set 'album', 'artist', or 'playlist' if the user explicitly names that kind.",
      "name": "Media type",
      "required": false,
      "selector": {
        "select": {
          "options": [
            "track",
            "artist",
            "album",
            "playlist"
          ]
        }
      }
    },
    "query": {
      "description": "What to play: a song title, artist, album, or playlist name.",
      "name": "Query",
      "required": true,
      "selector": {
        "text": {}
      }
    }
  },
  "icon": "mdi:music",
  "max": 5,
  "mode": "queued",
  "object_id": "play_music",
  "sequence": [
    {
      "action": "rest_command.resolver_command",
      "continue_on_error": true,
      "data": {
        "intent": "music",
        "params": {
          "media_type": "{{ media_type | default('', true) }}",
          "query": "{{ query | default('', true) }}"
        }
      },
      "response_variable": "r"
    },
    {
      "variables": {
        "resp": {
          "chat_text": "{{ r.content.chat_text if (r is defined and r.content is defined and r.content.chat_text is defined) else 'Sorry, I could not reach the music system.' }}"
        }
      }
    },
    {
      "response_variable": "resp",
      "stop": "done"
    }
  ]
}
